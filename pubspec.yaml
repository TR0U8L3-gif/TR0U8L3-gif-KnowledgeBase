name: knowledge_base
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.7+1
  flutter_markdown_plus: ^1.0.5
  google_fonts: ^6.3.2
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/knowledge_base/
    - assets/knowledge_base/getting_started/
    - assets/asset_manifest.json
